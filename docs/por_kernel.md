# Proof-of-Resonance Kernel

The Proof-of-Resonance (PoR) kernel provides a minimal interface for generating
stable resonance signatures.

## Usage

```python
from por_kernel import resonate

signature = resonate("Echo")
print(signature)
```
